{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run HomePulse",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/router_monitor.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}